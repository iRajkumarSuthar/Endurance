# Endurance

## About

Endurance is a student application and document verification portal for study-abroad workflows. Students can upload required documents, the system runs automated authenticity checks, flags missing or rejected files, and shows application progress as the packet moves toward completion.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.
